import { TouchTrailDirective } from './touch-trail.directive';

describe('TouchTrailDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchTrailDirective();
    expect(directive).toBeTruthy();
  });
});
